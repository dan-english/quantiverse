name: Dynamic Sidebar Generation

on:
  push:
    branches:
      - '**'

jobs:
  reunite-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      statuses: write
    steps:
      - name: dynamic_navigation
        run: python /home/runner/work/quantiverse/.github/workflows/todo_List.py
